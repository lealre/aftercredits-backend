apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Prometheus's service name on the shared compose network.
    url: http://prometheus:9090
    isDefault: true
    editable: false
